# mtt_snapspace — wanted

1. Fix: when the control track does not exist and control mode is disabled, the control track still gets created (theoretically it should not).
2. Add support for FX inside containers.
3. Add a mode that works only on selected tracks.
4. Add a specific filter system, or a system to edit snapshots.
