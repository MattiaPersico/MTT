# mtt_WritePositionRelativeAsTakeMarker

## What it is

For each selected media item, finds the item on the topmost ancestor folder track that contains it and writes a take marker on the selected item recording its start position relative to that containing item, as `TimeOffset: <seconds>`.

## Ruled out

